import React from 'react';
import Header from '../../components/header';
import RandomPlanets from '../../components//random-planet';

const HomePage = () => {
    return (
        <div>
            < Header />
            < RandomPlanets />
        </div>
    );
};

export default HomePage;